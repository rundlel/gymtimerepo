This code was developed in Xcode V.8.2.1 for iOS 10.2

An Apple Developer account and provisioning profile is required in order to run this codebase in Xcode.

This application uses Firebase Libraries which were installed using CocoaPods. The needed libraries are in the Podfile of this project, run the following terminal commands to install:

cd gymtimerepo
pod init
pod install